const slides=[...document.querySelectorAll('.slide')];let i=0,busy=false;const fill=document.querySelector('.progress span');function update(){fill.style.width=((i+1)/slides.length*100)+'%'}function go(n){if(n<0||n>=slides.length||n===i||busy)return;busy=true;const old=slides[i];old.classList.remove('active');old.classList.add('exit');i=n;slides[i].classList.remove('exit');slides[i].classList.add('active');update();setTimeout(()=>{old.classList.remove('exit');busy=false},650)}function next(){go(i+1)}function prev(){go(i-1)}document.querySelector('#next').onclick=next;document.querySelector('#prev').onclick=prev;document.addEventListener('keydown',e=>{if(['ArrowRight','ArrowDown','PageDown',' '].includes(e.key)){e.preventDefault();next()}else if(['ArrowLeft','ArrowUp','PageUp'].includes(e.key)){e.preventDefault();prev()}else if(e.key==='Home')go(0);else if(e.key==='End')go(slides.length-1)});let x=0;document.addEventListener('touchstart',e=>x=e.changedTouches[0].screenX,{passive:true});document.addEventListener('touchend',e=>{let d=e.changedTouches[0].screenX-x;if(Math.abs(d)>50)d<0?next():prev()},{passive:true});update();
